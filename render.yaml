services:
  - type: web
    name: cropmind-static
    env: static
    buildCommand: npm --prefix frontend install && npm --prefix frontend run build
    staticPublishPath: ./frontend/build
    envVars:
      - key: PUBLIC_URL
        value: https://cropmind.onrender.com