services:
  - type: web
    name: cropmind
    env: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    plan: free